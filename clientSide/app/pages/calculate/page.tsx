import MenuCard from "@/app/components/menu/MenuCard";

const CalculatePage = () => {
  return (
    <div className="flex min-h-screen items-center justify-center font-sans">
      <MenuCard />
    </div>
  );
};

export default CalculatePage;
